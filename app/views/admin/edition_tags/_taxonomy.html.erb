<% taxons.each do |taxon| %>
<p>
  <label>
    <%= checkbox_for_taxonomy(@edition_tag_form.selected_taxons, taxon) %>

    <span><%= taxon.name %></span>
  </label>
</p>

  <% if taxon.children.present? %>
    <div class="topics">
      <%= render partial: "taxonomy", locals: {form: form, taxons: taxon.children} %>
    </div>
  <% end %>
<% end %>
